# frozen_string_literal: true

RSpec.describe Octodoggy do
  it "has a version number" do
    expect(Octodoggy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
